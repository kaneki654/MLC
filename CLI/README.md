# MLCleaner-CLI v0.3.0.1
### The Professional System Intelligence Suite (Terminal Edition)

MLCleaner-CLI is the high-performance, terminal-based counterpart to the MLCleaner GUI. It provides root-level system optimization, hyper-scanning, and process management directly from your console.

## 🚀 Key Features
- **Hyper Scan**: Recursively discover and clean 1TB+ of system junk.
- **Process Sentinel**: Real-time `htop`-style resource monitoring.
- **System Optimizer**: Internal `sudo` handling for RAM and Swap management.
- **Dev Hub**: Rapid maintenance for Docker, NPM, and Python environments.

## 📥 Installation
1. Ensure you have `python3` and `pip` installed.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Launch MLCleaner-CLI:
   ```bash
   python3 main.py
   ```

## 🛠️ Requirements
- `rich`
- `psutil`
